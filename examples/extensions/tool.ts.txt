import { defineTool } from "eve/tools";
import { z } from "zod";

export default defineTool({
  description: "Return a bounded greeting without external side effects.",
  inputSchema: z.object({ name: z.string().min(1).max(100) }),
  async execute({ name }) { return { greeting: `Hello, ${name}` }; },
});
