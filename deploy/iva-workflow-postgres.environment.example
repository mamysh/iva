# Reference for an externally managed durable Workflow backend.
# Normal Ubuntu/Debian self-host setup: `iva workflow-postgres enable` generates the ignored
# deploy/iva-workflow.environment with mode 0600 after discovering the real socket and service user.

WORKFLOW_TARGET_WORLD=@workflow/world-postgres
WORKFLOW_QUEUE_NAMESPACE=eve
WORKFLOW_POSTGRES_URL=postgresql:///iva_workflow?host=/var/run/postgresql
WORKFLOW_POSTGRES_JOB_PREFIX=iva_
WORKFLOW_POSTGRES_WORKER_CONCURRENCY=8
WORKFLOW_POSTGRES_MAX_POOL_SIZE=10
